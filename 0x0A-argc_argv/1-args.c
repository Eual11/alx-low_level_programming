#include <stdio.h>

/**
 * main - prints  the number of arguments passed to our program
 * @argc: arguments count
 * @argv: array of arguments
 * Return: 0
 */

int main(int argc, char *argv[])
{
	(void) argv;

	printf("%d\n", argc);

	return (0);
}
